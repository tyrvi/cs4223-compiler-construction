main;

    data:
        integer: a, x, y, z;          # The 4-digit year adjusted for Zeller's congruence.
        real: year, array[6];         # The month (1-12).
    
    algorithm:
        counting x downward 9 to 1;
                 print "x = ", x, !;
                 counting y upward 1 to x;
                          print "y = ", y, !;
                 end counting;
        end counting;

        print "end x = ", x, !;
        print "end y = ", y, !;
   
end main;