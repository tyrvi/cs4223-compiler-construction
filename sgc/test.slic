#===============================================================
# File--------calendar.slic
# Programmer--Bryan Crawley
# Project-----Slic demo
#
# This program displays a calendar for a chosen month. The
# user specifies the month and year as integers. The program
# responds with a calendar for that month.
#
# Note: This program only works for the Gregorian calendar.
#===============================================================

main;

    data:
        integer: x, y;          # The 4-digit year adjusted for Zeller's congruence.
        real: year, z[6];         # The month (1-12).
      
    algorithm:
        y := 2;
        x := y+4;
        year := -(x + 2.0);# + -1.0e-2;
        #print "x = ", x, !;
        print "year = ", year, !;
		#z[1] := year;
		#z[2] := x - 3 + 2.0 - z[1];
        #print "z[1] = ", z[1], !;
        #print "z[2] = ", z[2], !;
   
end main;
